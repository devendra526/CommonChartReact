const sortedPoints = [...contributionPoints].sort(
  (a, b) => (b.y ?? 0) - (a.y ?? 0)
);

const MAX_VISIBLE_ROWS = 8;

const visiblePoints = sortedPoints.slice(0, MAX_VISIBLE_ROWS);

const hiddenPoints = sortedPoints.slice(MAX_VISIBLE_ROWS);

const remainingCount = hiddenPoints.length;
