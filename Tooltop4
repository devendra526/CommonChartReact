const sortedPoints = [...contributionPoints].sort(
  (a, b) => (b.y ?? 0) - (a.y ?? 0)
);

const MAX_VISIBLE = 8;

const visiblePoints = sortedPoints.slice(0, MAX_VISIBLE);

const hiddenCount =
  Math.max(sortedPoints.length - MAX_VISIBLE, 0);

*************
